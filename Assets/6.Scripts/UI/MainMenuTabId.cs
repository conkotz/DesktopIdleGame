public enum MainMenuTabId
{
    /// <summary>Settings and other pages without a tab button — tab strip shows all inactive.</summary>
    None = -1,
    Character = 0,
    Skills = 1,
    Quest = 2,
    Upgrade = 3,
    WorldMap = 4,
}
